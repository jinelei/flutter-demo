import 'package:flutter/material.dart';

class MoiveComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text('second page'),
    );
  }
}
