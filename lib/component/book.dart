import 'package:flutter/material.dart';

class BookComponnet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text('other page'),
    );
  }
}
